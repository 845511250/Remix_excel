<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_black_background</item>
        <item name="colorPrimaryDark">@color/color_black_background</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DialogTransBackGround" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground" >@color/color_trans</item>
    </style>


</resources>
